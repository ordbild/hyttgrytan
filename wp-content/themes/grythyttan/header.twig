<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width">
        <title>Grythyttan stålmöbler</title>
        <link rel="stylesheet" href="{{theme.link}}/assets/css/normalize.css">
        <link rel="stylesheet" href="{{theme.link}}/assets/css/boilerplate.css">
        <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/jquery.slick/1.5.9/slick.css"/>
        <link rel="stylesheet" href="{{theme.link}}/assets/css/fonts.css">
        <link rel="stylesheet" href="{{theme.link}}/assets/css/style.css?ver=160310">
        <!--[if lt IE 9]>
<script src="{{theme.link}}/assets/js/modernizr-2.6.2-respond-1.1.0.min.js"></script>
        <![endif]-->
        <?php wp_head();?>
    </head>
    <body class="{{ body_class }}">

        <header class="site-header">
            <div class="inner clearfix">
                <div class="brand">
                    <div class="logo">
                        <a href="{{site.link}}">
                            <picture>
                            <source type="image/svg+xml" srcset="{{theme.link}}/assets/img/grythyttan-logo.svg">
                            <img src="{{theme.link}}/assets/img/grythyttan-logo.png" alt="Grythyttan stålmöbler">
                            </picture>
                        </a>
                    </div>
                    <div class="social-media-icons">
                        <ul>
                            <li class="social-media-icon icon-facebook">
                                <a href="#">
                                    <img src="{{theme.link}}/assets/img/icons/icon-fb-wo-box.svg" alt="Facebook">
                                </a> 
                            </li>
                            <li class="social-media-icon icon-twitter">
                                <a href="#">
                                    <img src="{{theme.link}}/assets/img/icons/icon-twitter.svg" alt="Twitter">
                                </a>    
                            </li>
                            <li class="social-media-icon icon-instagram">
                                <a href="#">
                                    <img src="{{theme.link}}/assets/img/icons/icon-instagram.svg" alt="Instagram">
                                </a> 
                            </li>
                        </ul>
                    </div>
                </div>
                
                <nav class="main-menu">
                    <ul>
                        {% for menu_item in menu_items %}

                            {% set classes = menu_item['classes'] %}
                            {% if active_menu_item == menu_item['name'] %}
                                {% set classes = 'active ' ~ classes %}
                            {% endif %}
                            <li class="main-menu-item">
                                <a href="{{ menu_item['link'] }}" class="button {{ classes }}" target="{{ menu_item['target'] }}">
                                    {{ menu_item['link_text'] }}
                                </a>
                            </li>

                        {% endfor %}
                    </ul>
                </nav>
            </div>
            <div class="header-border"></div>
        </header>

